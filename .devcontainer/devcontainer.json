{
  "name": "Batroun Race",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",

  "postCreateCommand": "npm install -g firebase-tools @anthropic-ai/claude-code && echo '\\nReady. Run \"claude\" to start the AI assistant, or \"python3 -m http.server 8000 --directory docs\" to preview the public page.\\n'",

  "forwardPorts": [8000, 5000],
  "portsAttributes": {
    "8000": { "label": "Public page preview" },
    "5000": { "label": "Firebase emulator" }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode",
        "toba.vsfire"
      ],
      "settings": {
        "files.eol": "\n",
        "editor.tabSize": 2
      }
    }
  },

  "remoteEnv": {
    "ANTHROPIC_API_KEY": "${localEnv:ANTHROPIC_API_KEY}"
  }
}
